% Aggregate prose document for PrimeTensor-NS.
%
% Every module under PrimeTensor/ has (or will have) a matching fragment
% under proof/ at the same relative path, with .lean replaced by .tex.
% Each fragment is a bare \section-level fragment, so it is pulled in with
% a plain \input and nothing else.
%
% The \input list is GENERATED, not maintained by hand: build.sh writes
% modules.tex from proof/order.txt, keeping the entries whose fragment
% exists.  order.txt is the topological order of the import DAG, which is
% what the repository's out.pdf renders; Depth is its first entry.  Adding a
% module therefore means adding one file, never editing this one.
%
% Build:  cd proof && ./build.sh --main

\documentclass[11pt]{article}
\input{preamble}

\title{PrimeTensor-NS\\[0.3em]
  \large A prose reading of the machine-checked development}
\author{}
\date{}

\begin{document}
\maketitle
\tableofcontents
\bigskip

% Generated by build.sh from order.txt; see the note above.
\InputIfFileExists{modules}{}{%
  \PackageError{main}{modules.tex missing -- build with ./build.sh --main}{}}

\end{document}
